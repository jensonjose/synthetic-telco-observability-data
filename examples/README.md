# Examples

The `small_dataset` folder is a tiny generated dataset that I keep in the repo for quick inspection.

Regenerate it with:

```bash
make generate-example
make validate-example
```

The files are synthetic and are meant for demos, tests, screenshots, and quick sanity checks.
